# Core numerical / imaging stack
numpy>=1.26,<3
Pillow>=10.0
scikit-image>=0.22
opencv-python-headless>=4.8
PyYAML>=6.0
matplotlib>=3.8

# Deep-learning perceptual metric (LPIPS).
# On Windows install a CUDA build of torch FIRST for GPU support, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# then install the remaining requirements. The CPU wheel from PyPI also works.
torch>=2.1
torchvision>=0.16
lpips>=0.1.4

# Testing
pytest>=7.4
